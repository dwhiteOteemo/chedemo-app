# Default values for xdemo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.


frontEnd:
  replicaCount: 1
  name: xdemo-frontend
  image:
    repository: davwhiteoteemo/xdemo-frontend
    tag: latest
    pullPolicy: Always
    serverName: xdemo-backend
    servicePort: 9090

  nameOverride: ""
  fullnameOverride: ""

  service:
    type: ClusterIP
    port: 80

  ingress:
    enabled: false
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    paths: []
    hosts:
      - chart-example.local
    tls: []
    #  - secretName: chart-example-tls
    #    hosts:
    #      - chart-example.local

  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #  cpu: 100m
    #  memory: 128Mi
    # requests:
    #  cpu: 100m
    #  memory: 128Mi

  nodeSelector: {}

  tolerations: []

  affinity: {}


backEnd:
  replicaCount: 1
  name: xdemo-backend
  image:
    repository: davwhiteoteemo/xdemo-backend
    tag: latest
    pullPolicy: Always

    url: "jdbc:mysql://xdemo-database/"
    service_port: "9090"
    dbName: xdemo
    driver: "org.gjt.mm.mysql.Driver"
    userName: xdemo
    password: xdemo
    db_descriptor: "jdbc/xdemo"
    report_cache: "false"
    charts_cache: "false"
    redis_server: localhost
    server_name: localhost

  nameOverride: ""
  fullnameOverride: ""

  service:
    type: ClusterIP
    port: 9090

  ingress:
    enabled: false
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    paths: []
    hosts:
      - chart-example.local
    tls: []
    #  - secretName: chart-example-tls
    #    hosts:
    #      - chart-example.local

  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #  cpu: 100m
    #  memory: 128Mi
    # requests:
    #  cpu: 100m
    #  memory: 128Mi

  nodeSelector: {}

  tolerations: []

  affinity: {}

database:
  replicaCount: 1
  name: xdemo-database
  image:
    repository: davwhiteoteemo/xdemo-database
    tag: latest
    pullPolicy: Always
    dbPassword: "xdemo"
    dbUser: "xdemo"
    dbName: "xdemo"
  nameOverride: ""
  fullnameOverride: ""

  service:
    type: ClusterIP
    port: 3306

  ingress:
    enabled: false
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    paths: []
    hosts:
      - chart-example.local
    tls: []
    #  - secretName: chart-example-tls
    #    hosts:
    #      - chart-example.local

  resources: {}
    # We usually recommend not to specify default resources and to leave this as a conscious
    # choice for the user. This also increases chances charts run on environments with little
    # resources, such as Minikube. If you do want to specify resources, uncomment the following
    # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
    # limits:
    #  cpu: 100m
    #  memory: 128Mi
    # requests:
    #  cpu: 100m
    #  memory: 128Mi

  nodeSelector: {}

  tolerations: []

  affinity: {}

